# S7 Validation, Audit, And Closeout Evidence

Status: approved by independent program audit cycle 2
Date: 2026-07-11
Stage-open commit: `62995bd`
Implementation commit: this S7 closeout commit

## Delivered Validation

S7 added two reusable validation harnesses:

- `app-templates/customer-portal/scripts/s7-route-state-check.mjs` executes all
  17 routes across all three profiles, direct guards, parameterized routes,
  shell structure, and 59 unique production DOM/state/action probes.
- `app-templates/customer-portal/scripts/s7-regression-check.mjs` executes the
  complete 16-command matrix and writes a machine-readable report with exact
  command, exit, duration, environment, coverage, visual, CMS, and integrity
  evidence.

Executable probes revealed and closed four real state gaps: Calendar empty,
Products loading, Profile fixture success feedback, and Support keyboard/error
handling. Checkout and Support remain synchronous and no longer claim a fake
pending state.

The visual verifier was hardened against bounded capture jitter without
changing comparison thresholds, accepted masks, unavailable inventories, final
capture hashes, persisted image hashes, DOM metrics, or the S4 packet.

## Final Regression Result

`evidence/artifacts/S7/regression-result.json` records:

```text
commands                         16/16 pass
verticals / profiles / routes   8 / 3 / 17
route-profile attempts          51 (45 enabled, 6 disabled)
private auth guards             33
state probes                    59/59 pass
console / network failures      0 / 0
CMS export runs                 2, byte-identical
visual rows                     62/62
visual modes                    29 strict-full / 31 source-effect-components / 2 contract-state
accepted changed pixels / RMS   0 / 0
skipped checks                  0
upload                          false
```

Environment: Node `v24.14.0`, Playwright `1.61.1`, Chrome
`150.0.7871.115`, viewports 390/768/1180/1440.

The report confirms identical before/after hashes for the design source,
historical S0-S6 evidence, and the S4 visual packet. It also confirms no
background or CMS transaction debris.

## Exact Command

```bash
export PLAYWRIGHT_NODE_MODULES=/Users/imighty/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules
export NODE_PATH=/Users/imighty/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules
export PLAYWRIGHT_EXECUTABLE_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'
/Users/imighty/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node \
  app-templates/customer-portal/scripts/s7-regression-check.mjs
```

The aggregator ran syntax/JSON checks, config, route/state, runtime/exported
route smoke, PIM, SEO, Care, activation, CMS/export, full visual verification,
post-generation JSON, whitespace, and design-source integrity. Every command
exited zero in the final operator run.

## Audit History

- Independent cycle 1 found the transient pending/Support keyboard defect.
- Remediation removed false pending contracts, unified Support interaction
  paths, added scoped validation, and added real interaction/readback probes.
- Operator full regression then exposed visual capture jitter. The verifier was
  hardened with bounded exact-pair rounds and canonical warmup-only exclusion.
- Two consecutive full visual verifies, a clean full S7 aggregator, an
  additional operator full aggregator, and independent focused revalidation
  passed.
- Independent cycle 2 returned **APPROVED**. See `../audits/A1.md`.

## Residuals

- The external Draft 2020-12 validator remains unavailable; the fail-closed
  local subset is fully exercised by S6/S7.
- `care.live` and sensitive Health live actions remain `not_opened` because no
  repository-proven endpoint, RBAC, consent, audit, secure-document, and
  authoritative readback contract exists.
- CMS artifacts were generated locally. No upload was performed.
