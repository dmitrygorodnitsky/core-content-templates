{
  "schemaVersion": 1,
  "purpose": "Manual CMS package for the signed-in snow-removal customer portal on the SNOWLIMITLESS staging tenant: Core OIDC sign-in, the customer Account gate, the live overview and properties, Contracts and the read-only profile",
  "template": {
    "code": "CUSTOMER_PORTAL_GRANITE_RIDGE_STAGING",
    "title": "Limitless Snow Removal | Customer Portal"
  },
  "runtime": {
    "vertical": "snow",
    "theme": "snow",
    "profile": "stormRetail",
    "routerMode": "hash",
    "defaultRoute": "overview",
    "enabledModules": [
      "overview",
      "properties",
      "proposals",
      "profile"
    ],
    "authMode": "required",
    "dataMode": "live",
    "errorMode": "error"
  },
  "account": {
    "organization": "SNOWLIMITLESS",
    "accountTypeCode": "SNOW_RESIDENTIAL_CUSTOMER,SNOW_COMMERCIAL_CUSTOMER,CUSTOMER",
    "coreApiBase": "/core",
    "accountApiBase": "/core-acct",
    "billApiBase": "/core-bill",
    "resourceApiBase": "/core-rm"
  },
  "auth": {
    "coreBase": "/core",
    "callbackPath": "/core/oauth2-callback.html",
    "returnStorageKey": "oidc-return-url",
    "logoutReturnStorageKey": "oidc-logout-return-url"
  },
  "serviceGeography": {
    "map": {
      "center": { "lat": 49.23, "lon": -122.98 },
      "zoom": 10
    },
    "zones": {
      "vancouver": { "lat": 49.2827, "lon": -123.1207 },
      "richmond": { "lat": 49.1666, "lon": -123.1336 },
      "surrey": { "lat": 49.1913, "lon": -122.849 },
      "burnaby": { "lat": 49.2488, "lon": -122.9805 },
      "coquitlam": { "lat": 49.2838, "lon": -122.7932 }
    }
  },
  "shell": {
    "experienceId": "granite-ridge-snow-staging",
    "brandName": "Limitless Snow Removal",
    "defaultMode": "light",
    "primaryCtaLabel": "Request a quote",
    "navigation": {
      "primary": "Home",
      "appointments": "Appointments",
      "calendar": "Calendar",
      "care": "Season log",
      "proposals": "Contracts",
      "support": "Support"
    },
    "requestFormUrl": "https://dev-1.servicewand.com/pages/SNOWLIMITLESS/request-quote",
    "signedOutDestination": "landing",
    "landingUrl": "https://dev-1.servicewand.com/pages/SNOWLIMITLESS/home",
    "logoutReturnUrl": "https://dev-1.servicewand.com/pages/SNOWLIMITLESS/home?portal=signed-out",
    "allowedNavOrigins": ["https://dev-1.servicewand.com"]
  },
  "constraints": [
    "The page must be served from dev-1.servicewand.com, so the Core discovery document, the registered /core/oauth2-callback.html redirect and every service base stay same-origin.",
    "The signed-in Core User resolves to exactly one SNOWLIMITLESS customer Account through Account.user before any private module loads. The Account type must be one of those listed in accountTypeCode: SNOW_RESIDENTIAL_CUSTOMER and SNOW_COMMERCIAL_CUSTOMER, which the snow quotation flow creates, and CUSTOMER. Core filters the types with one IN filter and the browser checks them again. No linked Account, more than one, an organization the User may not enter, or a failed read all stop at the account gate.",
    "Properties come from the tenant-level SNOW_REMOVAL_PROPERTY list and are filtered to the resolved Account in the browser. The staging role SW_FS_WS_CUSTOMER_PORTAL can read every property of the tenant, so this package is staging-only until Core scopes that read to the customer Account on the server.",
    "enabledModules lists only what has live data and a live design: overview, properties, Contracts and the read-only profile. The runtime resolves the customer Account for every entry that requires sign-in, so account is not listed. Another stormRetail module joins the list here once its live adapter and design exist; the exporter refuses a module the profile does not own.",
    "PORTAL_WEATHER_CLIENT_ID, PORTAL_WEATHER_CLIENT_SECRET, PORTAL_MAPS_API_KEY and PORTAL_MAPS_MAP_ID are operator-owned CMS parameters. They ship the placeholder #, which the runtime reads as not set, because CMS creates no page for a template whose parameters have no value. The Xweather pair and the Google browser key are visible in page source by design and must be restricted to the serving host.",
    "The CMS template never stores or authors a password, API key, access token, User id, Account id, role or authorization result. SNOWLIMITLESS names the deployment tenant; it does not select a customer.",
    "Contracts reads the customer's quote Orders and service agreements from Core's generic endpoints and opens the customer's own decisions as Core workflow events: marking a sent quote viewed, approving or declining a quote, and approving an agreement sent for approval. Each runs one at a time, checks the record's state first and counts only once a readback shows the new state. Service requests and support replies are not available.",
    "The public landing at landingUrl is the portal's anonymous face. A private route whose session check settles signed out replaces itself with the landing; a signed-in User whose customer Account gate ends in customer-not-linked, organization-forbidden or customer-forbidden replaces it with the landing and ?portal=no-access, without signing out, because the Core Auth session is shared with Core UI on the same origin. Sign-out returns to logoutReturnUrl through the shared Core callback. #/login stays the explicit sign-in entry and never leaves for the landing."
  ]
}
